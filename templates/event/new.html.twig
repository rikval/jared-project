{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Add event{% endblock %}

{% block body %}
    <h1>add a date</h1>

    {{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.tour) }}
    {{ form_row(form.venue) }}
    {{ form_row(form.beginAt) }}
    {{ form_row(form.endAt) }}
    {{ form_row(form.allowance) }}

    <div class="text-right">
        <button type="submit" class="btn btn-primary">
            Save
        </button>
    </div>
    {{  form_end(form) }}
{% endblock %}