{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Your contacts{% endblock %}

{% block body %}
    <div class="row">
        <a href="{{ path('contact_new') }}"
           class="btn btn-white">
            New Contact
        </a>
    </div>

    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header card-header-rose">
                    <h4 class="card-title ">Your Contacts</h4>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                            <tr>
                                <th class="text-rose">Name</th>
                                <th class="text-rose">Email</th>
                                <th class="text-rose">Phone</th>
                                <th class="text-rose">Website</th>
                                <th class="text-rose">Language</th>
                                <th class="text-rose">Location</th>
                                {#<th class="text-primary">Note</th>#}
                                <th class="text-rose">Options</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for contact in contacts %}
                                <tr>
                                    <td><a href="{{ path('contact_show', {"id" : contact.id}) }}">{{ contact.name }}</a></td>
                                    <td>{{ contact.email }}</td>
                                    <td>{{ contact.phone }}</td>
                                    <td>{{ contact.website }}</td>
                                    <td>{{ contact.language }}</td>
                                    <td>{{ contact.location.city }} - {{ contact.location.country }}</td>
                                    {#<td>{{ contact.note }}</td>#}
                                    <td>
                                        <a class="btn btn-white btn-link btn-sm" href="{{ path('contact_edit', {"id" : contact.id}) }}">
                                            <i class="material-icons text-rose">edit</i>
                                        </a>
                                        <a class="btn btn-white btn-link btn-sm" href="{{ path('contact_delete', {"id" : contact.id}) }}">
                                            <i class="material-icons text-rose">close</i>
                                        </a>
                                    </td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
